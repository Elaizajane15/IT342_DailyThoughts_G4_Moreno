# IT342 Daily Thoughts — Docs

## Overview

- Backend: Spring Boot app under `backend/`.
- Frontend: Static HTML under `web/` with local storage.

## Usage

- Open `web/index.html` in a browser to record entries.
- Entries are stored in local storage and listed with timestamps.

## Next

- Expand docs with setup instructions and API details if backend endpoints are added.
